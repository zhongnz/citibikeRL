# Configs Folder

Centralize experiment settings here instead of hardcoding values in notebooks/scripts.

## Planned config files
- `stations.yaml` — selected station IDs/names
- `environment.yaml` — capacity bounds, refill amount `K`, penalties
- `training.yaml` — learning rate, epsilon, gamma, episodes
- `evaluation.yaml` — rollout horizon, metrics, random seeds
