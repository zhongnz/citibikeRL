dqn_training:
  top_n_stations: 5
  station_ids: []
  train_fraction: 0.75
  test_start_day:
  episodes: 5000
  gamma: 0.99
  epsilon: 0.35
  epsilon_decay: 0.995
  epsilon_min: 0.05
  learning_rate: 0.0005
  batch_size: 64
  replay_capacity: 20000
  replay_warmup: 256
  hidden_dim: 64
  heuristic_bucket_size: 2
  target_update_interval: 100
  train_interval: 1
  gradient_clip: 5.0
  heuristic_exploration_bias: 0.5
  move_action_margin: 3.0
  double_dqn: true
  dueling: true
  seed: 7
